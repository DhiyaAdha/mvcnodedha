<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
</head>
<body>
  <h1>Edit User</h1>

  <form action="/users/{{user.id}}" method="POST">
    <label for="userId">ID Pengguna: {{getProperty user 'id_user'}}</label>
    <br>

    <label for="nama">Nama Pengguna:</label>
    <input type="text" id="nama" name="nama" value="{{getProperty user 'nama'}}" required><br>

    <label for="alamat">Alamat Pengguna:</label>
    <input type="text" id="alamat" name="alamat" value="{{getProperty user 'alamat'}}" required><br>

    <button type="submit">Update</button>
  </form>
  
</body>
</html>
