<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
</head>
<body>
  <h1>Edit User</h1>

  <form action="/users/{{user.id}}" method="POST">
    <label for="userId">ID Pengguna:</label>
    <input type="text" id="userId" name="userId" readonly value="{{user.id}}"><br>

    <label for="userName">Nama Pengguna:</label>
    <input type="text" id="userName" name="userName" value="{{user.nama}}" required><br>

    <label for="userAddress">Alamat Pengguna:</label>
    <input type="text" id="userAddress" name="userAddress" value="{{user.alamat}}" required><br>

    <label for="registrationTime">Waktu Terdaftar:</label>
    <input type="text" id="registrationTime" name="registrationTime" readonly value="{{user.createdAt}}"><br>

    <button type="submit">Update</button>
  </form>
  
</body>
</html>
